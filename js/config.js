// NASA NeoWs API configuration.
// -----------------------------------------------------------------------------
// Get your own FREE key (instant, no email confirmation needed) at
// https://api.nasa.gov and paste it below, replacing "DEMO_KEY".
//
// DEMO_KEY works out of the box so the site runs immediately, but it is heavily
// rate-limited (~30 requests/hour, 50/day) and shared with everyone, so you may
// hit a 429 error. Use your own key for the graded/live version.
//
// NOTE: in a pure front-end site this key ships in the browser bundle. That is
// expected and acceptable for NASA keys (free + rate-limited). Do NOT reuse this
// pattern for secret/billable API keys.
export const CONFIG = {
  API_KEY: "4LytnIpMaa7K2vekNZNKauMw1PyHywppl1igBGQG",
  BASE_URL: "https://api.nasa.gov/neo/rest/v1",
};
